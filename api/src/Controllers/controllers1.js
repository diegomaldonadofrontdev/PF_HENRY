const { Model1 } = require("../db");
const axios = require("axios");

const controller1 = async () => {
    const controller = 1
    await controller
    return
}

module.exports = {
    controller1
  };