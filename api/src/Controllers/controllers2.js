const { Model2 } = require("../db");
const axios = require("axios");

const controller2 = async () => {
    const controller = 1
    await controller
    return
}

module.exports = {
    controller2
  };